fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
python-multipart==0.0.9
python-dotenv==1.0.1

# Uncomment if you switch DATABASE_URL to postgresql:// for a production/
# multi-employee deployment (SQLite has no driver to install, so it's not
# listed above):
# psycopg2-binary==2.9.9
